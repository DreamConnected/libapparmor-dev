
#include <tunables/global>

@{SNAP_NAME}="snapcraft"
@{SNAP_REVISION}="1594"
@{PROFILE_DBUS}="snap_2esnapcraft_2esnapcraft"
@{INSTALL_DIR}="/{,var/lib/snapd/}snap"

profile "snap.snapcraft.snapcraft" (attach_disconnected,mediate_deleted,complain) {
  # set file rules so that exec() inherits our profile unless there is
  # already a profile for it (eg, snap-confine)
  / rwkl,
  /** rwlkm,
  /** pix,

  capability,
  change_profile,
  dbus,
  network,
  mount,
  remount,
  umount,
  pivot_root,
  ptrace,
  signal,
  unix,


}
