
#include <tunables/global>

@{SNAP_NAME}="aws-cli"
@{SNAP_REVISION}="133"
@{PROFILE_DBUS}="snap_2eaws_2dcli_2eaws"
@{INSTALL_DIR}="/{,var/lib/snapd/}snap"

profile "snap.aws-cli.aws" (attach_disconnected,mediate_deleted,complain) {
  # set file rules so that exec() inherits our profile unless there is
  # already a profile for it (eg, snap-confine)
  / rwkl,
  /** rwlkm,
  /** pix,

  capability,
  change_profile,
  dbus,
  network,
  mount,
  remount,
  umount,
  pivot_root,
  ptrace,
  signal,
  unix,


}
